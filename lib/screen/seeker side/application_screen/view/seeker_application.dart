import 'package:flutter/material.dart';

class SeekerApplicationScreen extends StatelessWidget {
  const SeekerApplicationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title:const Text("Seeker Application"),
      ),
    );
  }
}